name: כתבת יום ראשון — המדריך לרוכש ולמוכר

on:
  schedule:
    # 05:30 UTC בכל יום ראשון (בהפרש קל מהטורים האחרים).
    - cron: '30 5 * * 0'
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  write:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: התקנת תלות
        run: npm install @anthropic-ai/sdk

      - name: ייצור כתבת השבוע
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node brokerage/scripts/generate-article.mjs

      - name: פתיחת Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: 'המדריך לרוכש ולמוכר — כתבת השבוע'
          title: 'המדריך לרוכש ולמוכר — כתבת השבוע'
          body: |
            כתבת תיווך שבועית חדשה נוצרה אוטומטית.
            בדקו את התוכן והתמונה, ואז מזגו כדי לפרסם.
          branch: weekly/brokerage-article
          add-paths: |
            src/content/brokerage/*.js
