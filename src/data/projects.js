/* ============================================================
   פרויקטים — כל הפרויקטים מנוהלים כעת אך ורק דרך מערכת הניהול (CMS / Supabase).
   אין יותר פרויקטי דמו מקודדים-קשיח: מה שנמחק בניהול נעלם מהאתר, ושום
   פרויקט "ישן" לא מופיע כ-fallback. הרשימה הריקה נשמרת לתאימות עם ייבואים
   קיימים (projects / getProject) — הם פשוט יקבלו ריק עד שה-CMS נטען.
   ============================================================ */

export const projects = []

export function getProject(slug) {
  return projects.find((p) => p.slug === slug)
}

export default projects
